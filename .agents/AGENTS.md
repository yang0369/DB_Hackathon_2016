# Workspace Rules

- Do NOT launch web servers, dev servers, or long-running background tasks automatically.
- Provide the exact shell command to the user so they can run dev servers manually in their terminal.
